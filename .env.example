# 复制成 .env 再改（.env 不会被提交到 git）
#
# 服务器上：cp .env.example .env && 改好下面几项 && ./deploy.sh
# 本地开发：只有 ADMIN_PASSWORD 和 GUESTBOOK_SALT 是必需的。

# ---- 站点地址（**构建期**变量，很重要）----
# canonical 链接、RSS、sitemap、分享卡片（OG）全由它生成。
# 改它必须重新 build，光重启容器没用。
# 还没有域名就先写 http://服务器IP
SITE_URL=https://your-domain.com

# 反向代理对外提供的域名。填了就走 HTTPS（Caddy 自动签证书并续期），
# 留空则退化成 :80 明文，用 http://IP 访问。
SITE_DOMAIN=your-domain.com

# 申请 HTTPS 证书用的联系邮箱（可选，建议填：证书出问题能收到通知）
ACME_EMAIL=you@example.com

# ---- 管理页 /admin 的密码 ----
# 上线前务必改成你自己的，建议 12 位以上。
# deploy.sh 会直接拒绝 change-me* 和已知的弱密码。
ADMIN_PASSWORD=change-me-please

# 用来计算访客 IP 的哈希（不存明文 IP）。随便一串随机字符即可，
# 但**上线后不要改**：改了以后所有点赞/表情的去重记录都会失效（等于重置）。
GUESTBOOK_SALT=change-me-too

# 留言审核开关：on = 先审核后显示（推荐上线用）；off = 提交即显示。
MODERATION=on

# 数据库文件位置（容器内路径，对应挂载的 ../data）
GUESTBOOK_DB=data/guestbook.db

# 备份相关：docker-compose 里已设好；只有直接在宿主机跑备份脚本时才需要
# BACKUP_DIR=backups
# KEEP=14
