# 仓库里统一存 LF，检出时按平台自动转换，避免整文件「被改动」的假 diff
* text=auto

# Windows 脚本保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# shell 脚本强制 LF：即使被直接拷到 Linux（不走 git），也不能带 CR，
# 否则会报 "$'\r': command not found" 这类莫名其妙的错
*.sh text eol=lf

# 二进制文件不要碰
*.jpg binary
*.jpeg binary
*.png binary
*.webp binary
*.ico binary
